<?xml version="1.0" encoding="utf-8"?>
<doc>
  <members>
    <member name="UpdateAsync(String,String,RequestContent,String,RequestContext)">
<example>
This sample shows how to call UpdateAsync with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new FavRulesClient(endpoint, credential);

var data = new {};

Response response = await client.UpdateAsync("<accountId>", "<assetId>", RequestContent.Create(data));

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result[0].ToString());
]]></code>
This sample shows how to call UpdateAsync with all parameters and request content, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new FavRulesClient(endpoint, credential);

var data = new {
    modelVersion = "<modelVersion>",
    dataSourceId = "<dataSourceId>",
    dataSourceFqn = "<dataSourceFqn>",
    favouriteFields = new[] {
        "<String>"
    },
    schemaAtSource = new {
        schemaProvider = "CATALOG",
        schema = "<schema>",
        refreshedAt = 1234L,
    },
    schemaReference = new {
        key = new {
            key = "<String>",
        },
    },
    dataSourceType = "CATALOG_ASSET",
    rules = new[] {
        new {
            id = "<id>",
            name = "<name>",
            description = "<description>",
            status = "INCOMPLETE",
            level = "ASSET",
            type = "NOTNULL",
            tags = new[] {
                "<String>"
            },
            typeProperties = new {},
        }
    },
};

Response response = await client.UpdateAsync("<accountId>", "<assetId>", RequestContent.Create(data), "<dataSourceFQN>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result[0].ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the request payload.

Request Body:

Schema for <c>DataQualityRules</c>:
<code>{
  modelVersion: string, # Optional. The DataQualityRules ModelVersion
  dataSourceId: string, # Optional. The DataQualityRules DataSourceId
  dataSourceFqn: string, # Optional. The DataQualityRules DataSourceFQN
  favouriteFields: [string], # Optional. The DataQualityRules FavouriteFields
  schemaAtSource: {
    schemaProvider: &quot;CATALOG&quot; | &quot;DATAQUALITY_SCAN&quot;, # Optional. The SourceSchema SchemaProvider
    schema: string, # Optional. The SourceSchema Schema
    refreshedAt: number, # Optional. The SourceSchema RefreshedAt
  }, # Optional. The SourceSchema Model
  schemaReference: Dictionary&lt;string, Dictionary&lt;string, string&gt;&gt;, # Optional. The DataQualityRules SchemaReference
  dataSourceType: &quot;CATALOG_ASSET&quot; | &quot;FLOWLET&quot;, # Optional. The DataQualityRules DataSourceType
  rules: [Rule], # Optional. The DataQualityRules Rules
}
</code>

</remarks>
    </member>
    <member name="Update(String,String,RequestContent,String,RequestContext)">
<example>
This sample shows how to call Update with required parameters and parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new FavRulesClient(endpoint, credential);

var data = new {};

Response response = client.Update("<accountId>", "<assetId>", RequestContent.Create(data));

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result[0].ToString());
]]></code>
This sample shows how to call Update with all parameters and request content, and how to parse the result.
<code><![CDATA[
var credential = new DefaultAzureCredential();
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new FavRulesClient(endpoint, credential);

var data = new {
    modelVersion = "<modelVersion>",
    dataSourceId = "<dataSourceId>",
    dataSourceFqn = "<dataSourceFqn>",
    favouriteFields = new[] {
        "<String>"
    },
    schemaAtSource = new {
        schemaProvider = "CATALOG",
        schema = "<schema>",
        refreshedAt = 1234L,
    },
    schemaReference = new {
        key = new {
            key = "<String>",
        },
    },
    dataSourceType = "CATALOG_ASSET",
    rules = new[] {
        new {
            id = "<id>",
            name = "<name>",
            description = "<description>",
            status = "INCOMPLETE",
            level = "ASSET",
            type = "NOTNULL",
            tags = new[] {
                "<String>"
            },
            typeProperties = new {},
        }
    },
};

Response response = client.Update("<accountId>", "<assetId>", RequestContent.Create(data), "<dataSourceFQN>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result[0].ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the request payload.

Request Body:

Schema for <c>DataQualityRules</c>:
<code>{
  modelVersion: string, # Optional. The DataQualityRules ModelVersion
  dataSourceId: string, # Optional. The DataQualityRules DataSourceId
  dataSourceFqn: string, # Optional. The DataQualityRules DataSourceFQN
  favouriteFields: [string], # Optional. The DataQualityRules FavouriteFields
  schemaAtSource: {
    schemaProvider: &quot;CATALOG&quot; | &quot;DATAQUALITY_SCAN&quot;, # Optional. The SourceSchema SchemaProvider
    schema: string, # Optional. The SourceSchema Schema
    refreshedAt: number, # Optional. The SourceSchema RefreshedAt
  }, # Optional. The SourceSchema Model
  schemaReference: Dictionary&lt;string, Dictionary&lt;string, string&gt;&gt;, # Optional. The DataQualityRules SchemaReference
  dataSourceType: &quot;CATALOG_ASSET&quot; | &quot;FLOWLET&quot;, # Optional. The DataQualityRules DataSourceType
  rules: [Rule], # Optional. The DataQualityRules Rules
}
</code>

</remarks>
    </member>
  </members>
</doc>